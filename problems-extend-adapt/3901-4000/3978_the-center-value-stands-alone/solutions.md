# Solutions — The Center Value Stands Alone

## Frequency scan

Compute the middle index with integer division and count occurrences of the
middle value across the whole array. The answer is true exactly when that
count is one.

The middle value is the only value whose frequency matters.

**Complexity:** `O(n)` time, `O(1)` space.
