pub struct EncodedRunIterator;

impl EncodedRunIterator {
    pub fn new(encoding: Vec<i32>) -> Self {
        panic!("TODO")
    }

    pub fn consume(&mut self, n: i32) -> i32 {
        panic!("TODO")
    }
}
