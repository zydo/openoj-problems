impl Solution {
    pub fn apply_text_substitutions(
        s: String,
        indices: Vec<i32>,
        sources: Vec<String>,
        targets: Vec<String>,
    ) -> String {
        panic!("TODO")
    }
}
