class PatchableGrid {
    constructor(rectangle: number[][]) {}

    updatePatch(row1: number, col1: number, row2: number, col2: number, newValue: number) {}

    getValue(row: number, col: number): number {}
}
