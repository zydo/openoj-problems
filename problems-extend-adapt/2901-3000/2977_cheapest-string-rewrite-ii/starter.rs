impl Solution {
    pub fn cheapest_rewrite(
        source: String,
        target: String,
        original: Vec<String>,
        changed: Vec<String>,
        cost: Vec<i32>,
    ) -> i64 {
        panic!("TODO")
    }
}
