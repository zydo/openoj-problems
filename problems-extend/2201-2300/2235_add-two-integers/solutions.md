# Solutions — Add Two Integers

## Add them

The sum of the two integers is their sum: one addition per language's native
integer type. The constraint window (`[-100, 100]`) keeps every input and
output far inside 32-bit range, so no overflow handling is needed anywhere.

**Complexity:** `O(1)` time, `O(1)` space.
