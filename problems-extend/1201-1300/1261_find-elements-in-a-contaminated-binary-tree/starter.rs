pub struct FindElements;

impl FindElements {
    pub fn new(root: Option<Box<TreeNode>>) -> Self {
        panic!("TODO")
    }

    pub fn find(&mut self, target: i32) -> bool {
        panic!("TODO")
    }
}
