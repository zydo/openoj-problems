pub struct Codec;

impl Codec {
    pub fn new() -> Self {
        panic!("TODO")
    }

    pub fn encode(&mut self, strs: Vec<String>) -> String {
        panic!("TODO")
    }

    pub fn decode(&mut self, s: String) -> Vec<String> {
        panic!("TODO")
    }
}
