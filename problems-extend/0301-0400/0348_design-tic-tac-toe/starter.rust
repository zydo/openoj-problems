pub struct TicTacToe;

impl TicTacToe {
    pub fn new(n: i32) -> Self {
        panic!("TODO")
    }

    pub fn make_move(&mut self, row: i32, col: i32, player: i32) -> i32 {
        panic!("TODO")
    }
}
