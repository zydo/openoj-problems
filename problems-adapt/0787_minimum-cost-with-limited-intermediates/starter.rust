impl Solution {
    pub fn minimum_limited_route_cost(
        nodeCount: i32,
        links: Vec<Vec<i32>>,
        source: i32,
        target: i32,
        maxIntermediates: i32,
    ) -> i32 {
        panic!("TODO")
    }
}
