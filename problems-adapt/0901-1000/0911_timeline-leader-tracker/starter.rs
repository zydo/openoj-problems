pub struct TimelineLeaderTracker;

impl TimelineLeaderTracker {
    pub fn new(labels: Vec<i32>, times: Vec<i32>) -> Self {
        panic!("TODO")
    }

    pub fn leaderAt(&mut self, t: i32) -> i32 {
        panic!("TODO")
    }
}
