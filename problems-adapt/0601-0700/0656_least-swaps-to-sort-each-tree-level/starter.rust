#[derive(PartialEq, Eq, Clone, Debug)]
pub struct TreeNode {
    pub val: i32,
    pub left: Option<Box<TreeNode>>,
    pub right: Option<Box<TreeNode>>,
}

impl Solution {
    pub fn least_level_swaps(root: Option<Box<TreeNode>>) -> i32 {
        panic!("TODO")
    }
}
