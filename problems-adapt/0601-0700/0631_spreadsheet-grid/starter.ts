class SpreadsheetGrid {
    constructor(height: number, width: string) {}

    set(row: number, column: string, val: number) {}

    get(row: number, column: string): number {}

    sum(row: number, column: string, numbers: string[]): number {}
}
