# Solutions — The Root Carries the Child Sum

## Compare the root against its two children

The tree has exactly three nodes, so the check is a direct comparison of
the root's value against the sum of its two children's values — no
traversal, no accumulation.

**Complexity:** `O(1)` time, `O(1)` space.
