pub struct JobBoard;

impl JobBoard {
    pub fn new(jobs: Vec<Vec<i32>>) -> Self {
        panic!("TODO")
    }

    pub fn post(&mut self, userId: i32, jobId: i32, priority: i32) {
        panic!("TODO")
    }

    pub fn reprioritize(&mut self, jobId: i32, newPriority: i32) {
        panic!("TODO")
    }

    pub fn withdraw(&mut self, jobId: i32) {
        panic!("TODO")
    }

    pub fn runTop(&mut self) -> i32 {
        panic!("TODO")
    }
}
