#[derive(PartialEq, Eq, Clone, Debug)]
pub struct TreeNode {
    pub val: i32,
    pub left: Option<Box<TreeNode>>,
    pub right: Option<Box<TreeNode>>,
}

impl Solution {
    pub fn count_paths_with_sum(root: Option<Box<TreeNode>>, targetSum: i32) -> i32 {
        panic!("TODO")
    }
}
