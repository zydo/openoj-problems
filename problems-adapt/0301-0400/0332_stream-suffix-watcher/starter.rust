pub struct SuffixWatcher;

impl SuffixWatcher {
    pub fn new(words: Vec<String>) -> Self {
        panic!("TODO")
    }

    pub fn feed(&mut self, letter: String) -> bool {
        panic!("TODO")
    }
}
