class NewsBoard {
    constructor() {}

    postMessage(userId: number, messageId: number) {}

    getFeed(userId: number): number[] {}

    follow(followerId: number, followeeId: number) {}

    unfollow(followerId: number, followeeId: number) {}
}
