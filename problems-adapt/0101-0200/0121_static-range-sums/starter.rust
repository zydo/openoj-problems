pub struct StaticRanges;

impl StaticRanges {
    pub fn new(nums: Vec<i32>) -> Self {
        panic!("TODO")
    }

    pub fn rangeSum(&mut self, left: i32, right: i32) -> i64 {
        panic!("TODO")
    }
}
