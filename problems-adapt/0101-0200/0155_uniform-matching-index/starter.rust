pub struct IndexSampler;

impl IndexSampler {
    pub fn new(nums: Vec<i32>) -> Self {
        panic!("TODO")
    }

    pub fn drawIndex(&mut self, target: i32) -> i32 {
        panic!("TODO")
    }
}
