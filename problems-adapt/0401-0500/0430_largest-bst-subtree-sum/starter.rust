#[derive(PartialEq, Eq, Clone, Debug)]
pub struct TreeNode {
    pub val: i32,
    pub left: Option<Box<TreeNode>>,
    pub right: Option<Box<TreeNode>>,
}

impl Solution {
    pub fn largest_bst_subtree_sum(root: Option<Box<TreeNode>>) -> i64 {
        panic!("TODO")
    }
}
