impl Solution {
    pub fn course_prerequisite_queries(
        courseCount: i32,
        prerequisites: Vec<Vec<i32>>,
        queries: Vec<Vec<i32>>,
    ) -> Vec<bool> {
        panic!("TODO")
    }
}
