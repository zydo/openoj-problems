class AuctionSystem {
    constructor() {}

    addBid(userId: number, itemId: number, bidAmount: number) {}

    updateBid(userId: number, itemId: number, newAmount: number) {}

    removeBid(userId: number, itemId: number) {}

    getHighestBidder(itemId: number): number {}
}
