# Solutions — Sum of Compatible Numbers in Range I

The absolute-difference rule gives one short bounded interval. Iterate its positive values and add exactly those whose bitwise AND with n is zero.

## Direct range enumeration

The absolute-difference rule gives one short bounded interval. Iterate its positive values and add exactly those whose bitwise AND with n is zero.

**Complexity:** `O(k) time, O(1) space`.
