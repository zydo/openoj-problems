## 387 — Sort Items by Groups Respecting Dependencies

- New id / title / slug: 387 / Grouped Items in Dependency Order / `grouped-items-in-dependency-order`
- Old → new API: `sortItems` → `orderGroupedItems` (go `orderGroupedItems`, rust `order_grouped_items`, ts `orderGroupedItems`); parameters `n`, `m`, `group` kept, `beforeItems` → `prerequisites`
- Core algorithm / difficulty: two-level topological sort (Kahn over groups, then Kahn inside each group) / H4 (unchanged)
- Statement rewritten from spec: yes (contiguity + prerequisite rules restated; "any solution accepted" kept)
- Examples newly constructed: yes (structure-preserving: yes — the figure's 8-row table and 8-box order strip keep their geometry)
  - `n=8, m=2, group=[-1,1,0,0,-1,1,0,-1]` with prereqs `[[4],[7],[6],[2],[],[1],[],[]]` → `[6,2,3,4,0,7,1,5]` (expected produced by the adapted reference itself — exact comparison, deterministic order); same input plus `[2]` on item 6 → `[]` (cycle 6→2→6 inside group 0)
- Constraints: domain unchanged, presentation rewritten
- Skeletons regenerated: all 7
- Figures: regenerated — example-1.svg: all 8 table rows (item/group/prerequisite), the column header, and the order strip relabeled `[6,2,3,4,0,7,1,5]` with per-box tints recomputed (blue×3, grey×3, amber×2); table/box geometry untouched
- Gates: check ✓ (per-bundle static clean) verify ✓ (7/7 languages, 16/16 cases) sandbox pending (batch) compatibility ✓ stale ✓ overlap ✓
- Sandbox: function kind, deferred to batch run

### Notes

- `comparison: exact` although the statement promises "any valid ordering":
  expected values are the reference's deterministic output, so public cases
  were generated by importing the adapted `solution.py` (private group ids
  and the LIFO queue make hand prediction unreliable — first manual trace
  was wrong).
- Figure regeneration trap: an `fill="#[0-9a-f]+"` regex silently misses the
  blue stroke `#4169E1` (uppercase E) — the two stale labels survived one
  pass; eyeball the final SVG, not just the counts.
- Overlap gate caught the alt text: nearly copied figure captions count as
  prose; rewritten to a fresh sentence.
