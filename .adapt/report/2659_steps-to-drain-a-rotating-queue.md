## 2659 — Make Array Empty

- New id / title / slug: 2659 / Steps to Drain a Rotating Queue / `steps-to-drain-a-rotating-queue`
- Old → new API: `countOperationsToEmptyArray` → `stepsToDrainQueue` (go `stepsToDrainQueue`, rust `steps_to_drain_queue`, ts `stepsToDrainQueue`); parameter `nums` kept
- Core algorithm / difficulty: removals in value order, cyclic-arc survivor counting with a Fenwick tree + k-th binary descent / H4 (unchanged)
- Statement rewritten from spec: yes — "array" with front/end operations reframed as a rotating queue
- Examples newly constructed: yes (structure-preserving: n/a — no figure)
  - `[2,5,1] → 5` (rotate twice before the first removal), `[-3,7,0,4] → 5` (immediate removal, then a wrap), `[6,8,10] → 3` (ascending input drains without rotation)
- Constraints: domain unchanged (1 ≤ n ≤ 10⁵, ±10⁹ values, distinct), presentation rewritten
- Skeletons regenerated: all 7
- Figures: none
- Gates: check ✓ verify ✓ (7/7 languages, 20/20 cases) sandbox n/a compatibility ✓ stale ✓ overlap ✓

### Notes

- Several hidden cases are ~10⁵-element arrays (cases.json ≈ 2 MB); the
  scaffold copies them byte-identically and the judge handles them fine —
  just don't try to read cases.json whole into the transcript.
- Operation tables in the examples were generated by a step-by-step
  simulation script, not hand-traced.
