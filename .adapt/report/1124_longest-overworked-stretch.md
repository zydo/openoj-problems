# 1124 — Longest Overworked Stretch

Source: `1124_longest-well-performing-interval` → `1124_longest-overworked-stretch`

## Gates

| gate | result |
| --- | --- |
| compatibility | OK |
| stale | OK |
| overlap | OK |
| local judge | 7/7 languages, 17/17 cases each |

## API

`longestWPI` → `longestOverworkedStretch` (rust `longest_wpi` →
`longest_overworked_stretch`). The single parameter `hours` is unchanged,
so no parameter rename reaches the compatibility gate after merge and the
staged-source re-check that Part A requires for parameter renames does not
apply here.

## Figures

None — the source bundle carries no figures.

## Provenance of this report

The bundle was authored by the `wave-a-08` chunk agent, which was stopped
when Part A was wound down before it wrote its own fragment and report.
The bundle itself was complete: all seven starters, all seven solutions,
`statement.md`, `solutions.md`, `cases.json` and `problem.json` were
present and every gate and the local judge passed on re-run. The fragment
and this report were reconstructed by the main agent from the bundle and
the source rather than re-authored, so nothing here is a fresh claim —
the gate results above are from a verification run after the agent stopped.
