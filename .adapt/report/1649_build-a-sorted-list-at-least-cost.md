## 1649 — Create Sorted Array through Instructions

- New id / title / slug: 1649 / Build a Sorted List at Least Cost / `build-a-sorted-list-at-least-cost`
- Old → new API: `createSortedArray` → `leastInsertionCost` (go `leastInsertionCost`, rust `least_insertion_cost`, ts `leastInsertionCost`); parameter `instructions` → `arrivals`
- Core algorithm / difficulty: Fenwick tree keyed by value; per arrival `min(query(x-1), count - query(x))`, mod `10⁹ + 7` / H3 (unchanged)
- Statement rewritten from spec: yes — values arrive one at a time into a sorted list, each placement priced at the smaller of its below/above counts
- Examples newly constructed: yes (structure-preserving: n/a — no figure)
  - `[3,1,4,2] → 1` (only the last placement pays), `[7,2,7,1,3,6,2] → 5` (duplicates place free, middle-bound values pay 2 apiece), `[6,3,5,2,4,1] → 3` (ends are cheap, the middle is not)
- Constraints: domain unchanged, presentation rewritten
- Skeletons regenerated: all 7
- Figures: none
- Gates: check ✓ verify ✓ (7/7 languages, 16/16 cases) sandbox n/a compatibility ✓ stale ✓ overlap ✓

### Notes

- The per-step explanation traces were generated by a script (insertion simulation with `min(less, greater)` printed per step) and pasted verbatim — hand-writing six `min(a, b) = c` lines is exactly where a wrong expected value sneaks in.
- The source's in-description worked example ("inserting 3 into [1,2,3,5]") was rebuilt with the same arithmetic but inside my own sentence, since it teaches the cost rule and its numbers (1,2,3,5 / min(2,1)) are the cheapest way to show it.
