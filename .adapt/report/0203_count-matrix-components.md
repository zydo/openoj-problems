## 203 — Number of Provinces

- New id / title / slug: 203 / Count Matrix Components / `count-matrix-components`
- Old → new API: findCircleNum → countComponents, find_circle_num → count_components, isConnected → adjacency
- Core algorithm / difficulty: function (multi-solution) kind; see the bundle's solutions.md
- Statement rewritten from spec: yes (base-era bundle)
- Constraints: domain unchanged, presentation rewritten
- Figures: labels updated (example-1); dropped (example-2)
- Gates (re-run at renumbering): OK OK OK

### Notes

- Report backfilled during the 1–829 renumbering: this base-era bundle's
  original report was never written or was lost. Facts above come from the
  ledger; the gates were re-run fresh today and are green.
