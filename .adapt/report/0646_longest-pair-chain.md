# Longest Pair Chain

- Source: `0646_maximum-length-of-pair-chain` → `0646_longest-pair-chain`
- Kind: function
- API renames:
  - `findLongestChain` → `longestPairChain`
- Figures: none
- Gates: compatibility ✓ stale ✓ overlap ✓

### Note

Salvaged centrally: the quota window killed the agent between writing the bundle and generating starters. Starters generated here; all three gates green.

Recovered by the main agent after the five-hour quota window ended every
chunk agent mid-flight. The bundle itself was finished work; only the
bookkeeping the agent performs *after* a bundle passes — fragment, report,
and (for 0646) the generated starters — was missing.
