## 2435 — Paths in Matrix Whose Sum Is Divisible by K

- New id / title / slug: 2435 / Count Grid Paths With Divisible Sum / `count-grid-paths-with-divisible-sum`
- Old → new API: `numberOfPaths` → `countDivisiblePaths` (go `countDivisiblePaths`, rust `count_divisible_paths`, ts `countDivisiblePaths`); parameters `grid`, `k` kept (conventional); vocabulary path → route in statement prose
- Core algorithm / difficulty: DP of k residue counters per cell, rolling one vector per column, shift by the cell's own residue, answer = residue-0 entry mod 1e9+7 / H3 (unchanged)
- Statement rewritten from spec: yes
- Examples newly constructed: yes (structure-preserving: yes — same grid shapes and qualifying-route positions)
  - `[[8,2,5],[7,3,6],[1,4,9]], k=3 → 2` (routes RRDD sum 30 and DRRD sum 33 — exactly the two geometries the figures draw), `[[2,3]], k=5 → 1` (single route), `[[4,8,1,6],[7,2,9,3],[5,0,8,2]], k=1 → 10` (everything qualifies)
- Constraints: domain unchanged (m·n ≤ 5·10⁴, values ≤ 100, k ≤ 50), presentation rewritten
- Skeletons regenerated: all 7
- Figures: labels updated — all four SVGs relabelled; the solution figure's nine residue vectors recomputed by script for the new grid (corner vector [2 2 2]); geometry untouched
- Gates: check ✓ verify ✓ (7/7 languages, 17/17 cases) sandbox n/a compatibility ✓ stale ✓ overlap ✓

### Notes

- To keep the two highlighted routes in the figures geometrically valid, the
  new 3×3 grid was found by search: exactly RRDD and DRRD ≡ 0 (mod 3), the
  other four routes ≢ 0. A grid that flips *which* routes qualify would have
  forced a redraw of the highlighted cells.
- The vectors figure is denser than the example figures but still a pure
  text edit — the DP vectors were generated by a one-off script and pasted
  into the nine `<text>` nodes.
