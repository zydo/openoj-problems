# Part B blocked sources

Problems that cannot be honestly adapted until a central (main-agent or
owner) fix lands in the live tree. Hidden case data may not be
regenerated by adapting agents, so a source whose reference solution and
statement disagree stops here.

## 2167_minimum-time-to-remove-all-cars-containing-illegal-goods

Blocked 2026-08-18 by wave-b-06. **The live bundle's reference solution
contradicts its own statement, and the hidden data follows the solution,
not the statement.**

- The statement charges 1 minute per end removal and 2 per interior
  removal; under those rules the optimum is `n + minsub(s, +1, -1)`
  (brute-force verified; `check2167c.py`, 0 mismatches).
- `solution.py` implements `n + minsub(s, +2, -1)` — the encoding for an
  interior cost of 3, not 2.
- Hidden cases 15 and 16 carry expected values 19979 / 14982, which match
  the `+2/-1` model; the statement-faithful optima are 19839 / 14650
  (`check2167b.py`, DIVERGENT on both). Every other hidden case agrees
  under both models.

Needed centrally, whichever side is the intended problem: reconcile
statement ↔ oracle (fix the prose or the reference encoding), regenerate
the two affected hidden expecteds, and re-verify the live bundle. Only
then re-dispatch 2167 for adaptation. Evidence scripts:
`.localonly/wave-b-06/bf2167.py`, `check2167*.py`.
