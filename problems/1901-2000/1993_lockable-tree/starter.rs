pub struct LockableTree;

impl LockableTree {
    pub fn new(parent: Vec<i32>) -> Self {
        panic!("TODO")
    }

    pub fn lock(&mut self, num: i32, user: i32) -> bool {
        panic!("TODO")
    }

    pub fn unlock(&mut self, num: i32, user: i32) -> bool {
        panic!("TODO")
    }

    pub fn upgrade(&mut self, num: i32, user: i32) -> bool {
        panic!("TODO")
    }
}
