impl Solution {
    pub fn rob(nums: Vec<i32>) -> i32 {
        let (mut prev, mut cur) = (0i64, 0i64);
        for &x in &nums {
            let next = cur.max(prev + x as i64);
            prev = cur;
            cur = next;
        }
        cur as i32
    }
}
