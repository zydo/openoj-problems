# Solutions — Two Numbers, One Total

## Add them

The whole task is a single addition in each language's native integer type.
The constraint window (`[-100, 100]`) keeps every input and output far
inside 32-bit range, so no overflow handling is needed anywhere.

**Complexity:** `O(1)` time, `O(1)` space.
