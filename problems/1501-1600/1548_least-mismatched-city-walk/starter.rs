impl Solution {
    pub fn least_mismatched_walk(
        n: i32,
        roads: Vec<Vec<i32>>,
        names: Vec<String>,
        targetPath: Vec<String>,
    ) -> Vec<i32> {
        panic!("TODO")
    }
}
