pub struct GridVictoryTracker;

impl GridVictoryTracker {
    pub fn new(n: i32) -> Self {
        panic!("TODO")
    }

    pub fn place_mark(&mut self, row: i32, col: i32, player: i32) -> i32 {
        panic!("TODO")
    }
}
