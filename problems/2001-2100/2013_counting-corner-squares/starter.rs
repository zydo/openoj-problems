pub struct SquareCounter;

impl SquareCounter {
    pub fn new() -> Self {
        panic!("TODO")
    }

    pub fn add(&mut self, point: Vec<i32>) {
        panic!("TODO")
    }

    pub fn count(&mut self, point: Vec<i32>) -> i32 {
        panic!("TODO")
    }
}
