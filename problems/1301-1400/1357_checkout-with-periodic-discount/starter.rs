pub struct DiscountRegister;

impl DiscountRegister {
    pub fn new(n: i32, discount: i32, products: Vec<i32>, prices: Vec<i32>) -> Self {
        panic!("TODO")
    }

    pub fn get_bill(&mut self, product: Vec<i32>, amount: Vec<i32>) -> f64 {
        panic!("TODO")
    }
}
