pub struct AncestorFinder;

impl AncestorFinder {
    pub fn new(n: i32, parent: Vec<i32>) -> Self {
        panic!("TODO")
    }

    pub fn kthAncestor(&mut self, node: i32, k: i32) -> i32 {
        panic!("TODO")
    }
}
